function tambah(a: number, b: number): number {
  return a + b;
}

console.log(tambah(5, 5)); // Output: 12

function sapa(nama: string): void {
  console.log(`Halo, ${nama} !`);
}

// Tes
sapa("Budi");   // Output: Halo, Budi!
sapa("Ani");    // Output: Halo, Ani!

